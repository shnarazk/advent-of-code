[package]
name = "aoc_macro"
version = "0.2.6"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
syn = { verson = "^1.0", features = ["full"] }
quote = "^1.0"

[lib]
proc-macro = true
